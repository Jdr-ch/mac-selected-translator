# 项目工作规则

对于边界明确、且未要求完整加固或全量验证的实现任务，优先使用 `$implement-focused-feature`。用户明确验收、仓库硬性门禁和专项 Skill 的要求优先。

处理本项目的设计、实现、诊断、验证或 App 替换前，读取 `.agents/skills/maintain-mac-selected-translator/SKILL.md`，再按其路由只加载相关参考文件。该 Skill 不授权额外构建、安装、系统设置变更或真机操作；本轮用户请求决定动作范围。
